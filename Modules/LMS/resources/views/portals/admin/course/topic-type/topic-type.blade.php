<x-portal::course.topic-type.type actionType="{{ $actionType }}" :chapters="$chapters" type="{{ $type }}"
    topicId="{{ $topicId }}" chapterId="{{ $chapterId }}" courseId="{{ $courseId }}" :topic=$topic />
